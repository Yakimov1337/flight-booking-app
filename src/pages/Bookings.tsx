import React from 'react';
import FilteredBookingList from '../components/Booking/FilteredBookingList';

const Bookings: React.FC = () => {
  return (
    <FilteredBookingList/>
  );
};

export default Bookings;
